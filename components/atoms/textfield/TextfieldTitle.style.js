import styled from "styled-components";

const StyledTextfieldTitle = styled.div`
  display: flex;
  .tag-wrapper,
  .variant-wrapper {
    width: 100px;
    margin-right: 16px;
  }
  .title-wrapper {
    flex-grow: 1;
  }
`;

export {StyledTextfieldTitle};
