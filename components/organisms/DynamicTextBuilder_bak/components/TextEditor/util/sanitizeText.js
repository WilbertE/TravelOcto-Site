// const sanitizeText = function (rawText) {
//   var temp = document.createElement("div");
//   temp.innerHTML = rawText;
//   return temp.textContent || temp.innerText;
// };

// export {sanitizeText}