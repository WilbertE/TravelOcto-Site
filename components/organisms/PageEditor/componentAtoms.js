import {atom} from "recoil";

export const updateComponentState = atom({key: "updateComponentState", default: null});
export const addComponentState = atom({key: "addComponentState", default: null});
export const deleteComponentState = atom({key: "deleteComponentState", default: null});
