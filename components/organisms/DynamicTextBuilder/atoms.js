import {atom} from "recoil";

export const configuratorState = atom({key: "configuratorState", default: null});
export const segmentState = atom({key: "segmentState", default: null});
export const tagsState = atom({key: "tagsState", default: null});
export const addSegmentState = atom({key: "addSegmentState", default: null});
export const deleteSegmentState = atom({key: "deleteSegmentState", default: null});
